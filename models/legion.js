class Legion {
  constructor(id, title, players, maxPlayers, isActive) {
    this.id = id;
    this.title = title;
    this.players = players;
    this.maxPlayers = maxPlayers;
    this.isActive = isActive;
  }
}

export default Legion;
